{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DetalleAlimento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fechavencimiento</th>
                <th>Contenido</th>
                <th>Pesounitario</th>
                <th>Stock</th>
                <th>Reservado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('detallealimento_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fechaVencimiento %}{{ entity.fechaVencimiento|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.contenido }}</td>
                <td>{{ entity.pesoUnitario }}</td>
                <td>{{ entity.stock }}</td>
                <td>{{ entity.reservado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('detallealimento_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('detallealimento_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('detallealimento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
