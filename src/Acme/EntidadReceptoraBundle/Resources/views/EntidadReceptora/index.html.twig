{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EntidadReceptora list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Razonsocial</th>
                <th>Telefono</th>
                <th>Domicilio</th>
                <th>Latitud</th>
                <th>Longitud</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('entidadreceptora_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.razonSocial }}</td>
                <td>{{ entity.telefono }}</td>
                <td>{{ entity.domicilio }}</td>
                <td>{{ entity.latitud }}</td>
                <td>{{ entity.longitud }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('entidadreceptora_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('entidadreceptora_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('entidadreceptora_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
